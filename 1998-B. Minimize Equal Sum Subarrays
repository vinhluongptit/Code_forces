#include <bits/stdc++.h>
using namespace std;
using ll = long long;
int mod = 1e9 + 7;

int main() 
{
	int t;
	cin >> t;
	while(t--){
		int n;
		cin >> n;
		vector<int> v(n), pref(n);
		for(int i = 0; i < n; i++){
			cin >> v[i];
		}
		for(int i = 0; i < n-1; i++){
			cout << v[i+1] << " ";
		}
		cout << v[0];
		cout << endl;
	}
}
