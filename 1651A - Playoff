#include <bits/stdc++.h>
using namespace std;
using ll = long long;
 
int main() 
{
  int t;
  cin >> t;
  while(t--){
    int n;
    cin >> n;
      ll x = pow(2, n) - 1;
      cout << x;
    cout << endl;
  }
}
