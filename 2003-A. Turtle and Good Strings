#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() 
{
	int t;
	cin >> t;
	while(t--){
		int n;
		cin >> n;
		string s;
		cin >> s;
		char c = s[0];
		int ok = 0;
		if(s[n-1] != c){
			ok = 1;
		}
		if(ok) cout << "YES";
		else cout << "NO";
		cout << endl;
	}
}
