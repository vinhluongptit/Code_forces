#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() 
{
  int t;
  cin >> t;
  while(t--){
    int n;
    cin >> n;
    int a[n+1];
    ll sum = 0;
    for(int i = 1; i <= n; i++){
      cin >> a[i];
      sum += abs(a[i]);
    }
    cout << sum << endl;
    
    }
}
}
